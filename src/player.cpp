#include "player.h"

Player::Player(int scr, int lvl, int lf, bool slow, bool mag, bool las, bool expand, bool div):
    p_score(scr), p_level(lvl), p_lifes(lf),p_slow(slow),p_magnet(mag),p_laser(las),p_expand(expand),p_divide(div)
{
    
}